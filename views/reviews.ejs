<!DOCTYPE html>
<html lang='en'>
<head>
    <meta charset='UTF-8'>
    <meta name='viewport' content='width=device-width, initial-scale=1.0'>
    <title>Stave Blog </title>
    
</head>
<body>
    <header>
        <h3>Stave Coffee</h3> 
        <nav><ul><li><a href='/'>Back to Store</a></li></ul></nav>
    </header>
    <h3>Leave us a review!</h3>
    
    <div class='container'>
        
        <form action='/blog' method='POST'>
            <select name='userId'>
                <% for(let i=0; i<users.length; i++) { %>
                    <option value='<%= users[i]._id %>'><%= users[i].username %></option>
                    <% } %>
            </select></br>
            <label for='title'>Add a headline</label><br/>
            <input type='text'  name='title' placeholder='What is most important to know?' id='title'/><br/>
            <label for='review'>Write your review</label><br/>
            <textarea name='review' placeholder='What did you like or dislike? How did you brew this roast?' id='title'></textarea><br/>
            <label for='firstTime'>Is this your first time trying this roast?</label><br/>
            <input type='checkbox' name='firstTime'><br/>
            <input type='submit' value='Submit'>
        </form>

        <% for(let i=0; i<users.length; i++) { %>
            <h5><%= users[i].username %></h5>
            <h2><%= users[i].title %></h2></h1>
            <p><%= users[i].review %></p>
            <% if (users[i].firstTime === true) { %>
                <h6><%= users.username %> is a first time Stave Coffee drinker</h6>
            <% } else { %> <h6><%= users[i].name %> is a veteran Stave Coffee drinker</h6>
            <% } %>
            <ul>
                <li>
                    <a href='/blog/<%= users[i].id %>/edit'>Edit</a>
                </li>
                <li>
                    <form action='/blog/<%= users[i].id %>?_method=DELETE' method='POST'>
                    <input type="submit" value="DELETE"/>
                    </form>
                </li>
            </ul>
            
            
       <% } %>

    </div>
   
</body>
</html>