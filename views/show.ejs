<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Review Page</title>
</head>
<body>
    <div>
    <h5><%= reviews.title %></h5>
    <small>by<a href='/blog/<%= user._id %>'><%= user.username %></a></small>
    <p><%= reviews.review %></p>
        <% if (reviews.firstTime === true) { %>
    <h6><%= reviews.name %> is a first time Stave Coffee drinker</h6>
        <% } else { %> 
    <h6><%= reviews.name %> is a veteran Stave Coffee drinker</h6>
        <% } %>
    </div>
    <section><h2>More Posts From This Author:</h2>
        <ul>
            <% for (let i=0; i < user.reveiws.length; i++) { %>
                <li>
                    <a href='/blog/<%= user.reviews[i]._id %>'><%= user.reviews[i].title %></a>
                </li>
            <% } %>
        </ul>
    </section>
    <ul>
        <li>
            <a href='/blog/<%= reviews.id %>/edit'>Edit</a>
        </li>
        <li>
            <form action='/blog/<%= reviews.id %>?_method=DELETE' method='POST'>
            <input type="submit" value="DELETE"/>
            </form>
        </li>
    </ul>
</body>
</html>